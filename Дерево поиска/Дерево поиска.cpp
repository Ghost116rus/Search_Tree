// Дерево поиска.cpp : Этот файл содержит функцию "main". Здесь начинается и заканчивается выполнение программы.

#include <iostream>



int main()
{
	//Search_Tree* Root = nullptr;
	//Add_Node_NoR(Root, 22);

	//Add_Node_NoR(Root, 6);
	//Add_Node_NoR(Root, 6);
	//Add_Node_NoR(Root, 16);

	//Add_Node_NoR(Root, 46);
	//Add_Node_NoR(Root, 55);
	//Add_Node_NoR(Root, 42);
	//Add_Node_NoR(Root, 44);

	//symmetric(Root, 0);
	//std::cout << "\n\n";

	//Delete_Node_NoR(Root, 46);
	//symmetric(Root, 0);
	//std::cout << "\n\n";

	//Delete_Node_NoR(Root, 44);
	//symmetric(Root, 0);
	//std::cout << "\n\n";

	//Delete_Node_NoR(Root, 22);
	//symmetric(Root, 0);

	//clean_memory(Root);

}

